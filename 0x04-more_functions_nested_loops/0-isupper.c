#include "main.h"
/**
 * _isupper - check if it's uppercase
 * @c: the number to be checked
 * Return: 1 for uppercase and 0 for otherwise
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	return (0);

}
